import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'pruebaTaller',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
